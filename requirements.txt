# Simulation / general
numpy
scipy
matplotlib
pandas
seaborn

# ML detector (CNN Autoencoder — see docs/DECISIONS.md, architecture is settled, not CNN+LSTM)
tensorflow>=2.15
scikit-learn

# PQC (CRYSTALS-Dilithium) — see docs/PQC_SETUP.md before installing.
# python-oqs wraps the liboqs C library; you must build liboqs FIRST,
# then pip install this. It will fail otherwise.
liboqs-python

# Network-layer covert channel (network_covert_channel/) — packet crafting
# and pcap parsing. iperf3/tshark/tcpdump are used via subprocess when
# present on the host but are not Python deps; see
# network_covert_channel/README.md for what's actually available where.
scapy

# Dashboard
streamlit
plotly

# Monitoring (Grafana needs this exporter as its data source, see monitoring/)
prometheus_client

# Dev / testing
pytest
jupyter
