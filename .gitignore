# Python
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.python-deps/

# Large generated artifacts (keep results/ CSVs and plots, not raw logs)
*.log
*.h5
*.ckpt
results/*.npy

# pqc_auth demo-only state: plaintext keys + accumulated audit log, never committed
pqc_auth/.demo_state/
pqc_auth/.live_loop_state/

# local liboqs build tree (see docs/PQC_SETUP.md), never committed
liboqs/

# trained TensorFlow models (regenerate with python -m detector.evaluate_cnn_scan)
results/models/
