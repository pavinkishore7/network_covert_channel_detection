[pytest]
markers =
    integration: live tests that need root/CAP_NET_ADMIN and a real Linux
        network namespace (network_covert_channel's topology). Excluded by
        default -- see network_covert_channel/README.md for how to run
        them on a machine that actually has the required privileges.
addopts = -m "not integration"
